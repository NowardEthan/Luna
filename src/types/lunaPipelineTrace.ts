/** Trace estruturado do pipeline Luna Core (painel Atividade). */
export type LunaPipelineTrace = {
  intencao?: string
  complexidade?: string
  nivelRisco?: string
  politicaAcao?: string
  politicaTom?: string
  politicaModo?: string
  memoriaAcao?: string
  memoriaTipo?: string
  memoriaMotivo?: string
}
